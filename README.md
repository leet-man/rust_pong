# How to play
1. Inside terminal: `cargo new pong_rust --bin`, and `cd pong_rust`.
3. Inside crate: copy the Cargo.toml and copy the main module inside a `src` directory.
4. Run `cargo run` in terminal.
5. Enjoy!
